package com.pantrybase.api.user.domain;

/**
 * Enum representing different dietary preferences or restrictions.
 * Provisional until the Edamam label mapping is validated.
 */
public enum Diet {
    BALANCED,
    HIGH_FIBER,
    HIGH_PROTEIN,
    LOW_CARB,
    LOW_FAT,
    LOW_SODIUM
}
